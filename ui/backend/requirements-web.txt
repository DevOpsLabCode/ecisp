# The FastAPI web layer only. No engine, no test tooling -- this is what
# the production Docker image installs on top of the engine's own
# requirements.txt.
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
python-multipart==0.0.32
openpyxl==3.1.5

# Org-wide GitHub security scanner (app/orgscan/): GitHub REST API client,
# HTML->PDF report rendering (weasyprint needs pango/cairo -- installed via
# apt in the Dockerfile; PDF generation is a soft dependency, see
# org_scan_job.py's _write_reports, so its absence doesn't block the other
# four report formats).
httpx==0.28.1
weasyprint==69.0
defusedxml==0.7.1

# Runtime Defender containment state (app/db.py, app/runtimedefender/
# containment_models.py): the rule->response mapping and command queue need
# to survive a backend restart, unlike everything else in this app -- see
# db.py's docstring. psycopg is the Postgres driver for production
# deployments; SQLite (stdlib, no extra driver) is the local-dev/test
# default.
sqlalchemy==2.0.44
alembic==1.16.5
psycopg[binary]==3.2.10
